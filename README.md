# SG16 Children World — sg16children.com

Static ad landing page with full-screen promo video for **SG16 Children World**.

## Deploy (Cloudflare Pages)

1. Push to GitHub → connect repo in Cloudflare Pages (same as saiftechglobal.com).
2. **Build command:** *(empty)*
3. **Output directory:** `/`
4. Custom domain: `sg16children.com` + `www.sg16children.com`

## Local preview

```powershell
npx --yes serve .
```

Open http://localhost:3000

## Assets

- `assets/sg16-children-world.mp4` — ad video (IMG_5912)
